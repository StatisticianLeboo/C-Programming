#include <stdio.h>

/**
 * Program to count from zero to ten
 * @n - the a parameter
 * the program is using the do while loop
 */

int main ()
{
	int n;

	do {
	printf("%d \n", n);
	n++;
	}while(n<=10);
return 0;
}
